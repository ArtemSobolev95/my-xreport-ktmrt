meta:
  environment: docker
  toolchain:
    name: docker

run:
  containerPort: 8090
  persistenceMount: /data
  command: ./pocketbase serve --dir /data --httpAddr="0.0.0.0:8090" --enable-wal --logLevel=error